<Window x:Class="SportRadar.Client.Views.LiveScoreView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:SportRadar.Client.Views"
        mc:Ignorable="d" Background="Aqua"
        Title="LiveScoreView" Height="350" Width="600">
    <Grid>

        <Grid Background="CadetBlue">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="1*"></ColumnDefinition>
            </Grid.ColumnDefinitions>
            <Grid.RowDefinitions>
                <RowDefinition Height="1*"></RowDefinition>
                <RowDefinition Height="1*"></RowDefinition>
            </Grid.RowDefinitions>
            <Label Grid.Row="0" Grid.Column="0" Margin="20"   FontSize="25" HorizontalAlignment="Center" >LIVE SCORE</Label>
            <StackPanel Width="300" Height="auto" Orientation="Vertical" Grid.Column="0" Grid.Row="0">
                <ListView>

                </ListView>

                <Button HorizontalAlignment="Stretch" VerticalAlignment="Bottom" >Refresh</Button>
            </StackPanel>
        </Grid>


    </Grid>
</Window>
